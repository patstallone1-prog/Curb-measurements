# Working images are streamed and deleted; nothing here is worth versioning.
work/
*.jpg
*.jpeg
*.png
.venv/
__pycache__/
*.pyc
.env.local

# The published map. The rules above ignore images because the working captures are images;
# the icons GitHub Pages serves are not captures and have to survive that.
!docs/*.png
